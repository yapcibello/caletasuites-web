# Google Tag Manager — ID del contenedor (placeholder).
# Astro/Vite NO carga el .env del raíz del monorepo: este archivo vive en apps/www/.
# Las variables PUBLIC_* se inyectan en el cliente (import.meta.env.PUBLIC_GTM_ID).
# GA4 existente de Caleta Suites: propiedad 423687681 (se conecta vía este contenedor GTM).
PUBLIC_GTM_ID=GTM-XXXXXX
